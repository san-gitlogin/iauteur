# Channel Profile — fill ONCE; every video inherits this automatically
#
# This file ships UNFILLED. Nothing here is required to render — the defaults below work as-is —
# but filling it makes every future spec, upload kit and CTA come out in your voice without you
# repeating yourself. Fill it in your own clone: it is your identity, not the project's.
CHANNEL NAME: YOUR CHANNEL             # becomes brand.channel in specs, and the name in the upload kit
HANDLE: <fill: @handle>
LOGO FILE: channel_logo.png            # public/assets/channel_logo.png — ships as iAuteur's own mark. Overwrite
                                       # that file with your own square transparent PNG (~512px) and every spec
                                       # picks it up via brand.logo="img:channel_logo.png"
TAGLINE: <fill: e.g. "One tech concept, explained simply">
DEFAULT AUDIENCE: general              # beginner | dev | general
LANGUAGE: english
UPLOAD CADENCE: <fill: e.g. 2 long + 3 shorts weekly>
CTA VOICE: warm, zero begging: "if this saved you a search, subscribe"
SIGNATURE SIGN-OFF: <optional>
DEFAULT DESIGN PACK: moderndark        # standing default when the user doesn't specify; MAY repeat — vary background/screenplay/scene mix instead
DEFAULT VOICE: en-US-AvaMultilingualNeural # edge-tts; reads as a person rather than a synthesiser
BRANDED ANCHOR PHRASE: "In this video, <CHANNEL NAME> breaks down"   # verbatim opener of description paragraph 2, every video
SLOGAN: <fill: fixed subscribe-CTA slogan>
WATCH_NEXT: <fill: URL/title of current best performer — UPDATE whenever a video validates>
SOCIALS: <fill: links block>
NOTES: Thumbnails must use topic-relevant art (si: brand logo → user-supplied img: → set-piece still) — never a bare lucide glyph. Interview the user FIRST (LAW 0 in CLAUDE.md) before any video work.
